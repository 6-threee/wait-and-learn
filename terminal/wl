#!/bin/sh
# Agora grading wrapper. Usage: wl got | wl missed
# Add an alias so you can just type `wl got`:
#   echo "alias wl='$HOME/Desktop/wait-and-learn/terminal/wl'" >> ~/.zshrc
exec "$HOME/.bun/bin/bun" "$(dirname "$0")/grade.mjs" "$@"
